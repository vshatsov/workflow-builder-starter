/**
 * CREDENTIALS TYPE
 *
 * Defines the credential fields for your integration.
 */

export type [IntegrationName]Credentials = {
  [INTEGRATION_NAME]_API_KEY?: string;
  // Add other credential fields as needed
};
