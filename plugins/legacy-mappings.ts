/**
 * Legacy Action Mappings
 *
 * Maps old action IDs (labels) to new namespaced IDs.
 * Used for backward compatibility when loading older workflows.
 *
 * Format: { "Old Label": "integration/slug" }
 */
export const LEGACY_ACTION_MAPPINGS: Record<string, string> = {
  // Add mappings here as needed when renaming actions
  // Example: "Send Email": "resend/send-email",
};
