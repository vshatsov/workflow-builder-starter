# =============================================================================
# Workflow Builder Starter - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
#
# For Vercel deployments, set these in your project's Environment Variables.
# =============================================================================

# -----------------------------------------------------------------------------
# Database (Required)
# -----------------------------------------------------------------------------
# Option A: Vercel Postgres (recommended for deployed apps)
#   Create at: https://vercel.com/dashboard → Storage → Create Database → Postgres
#   The connection string will be automatically added to your project.
#
# Option B: Local Docker (for offline development)
#   Run: docker compose up -d
#   Then use the URL below:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/workflow

# -----------------------------------------------------------------------------
# Authentication (Required)
# -----------------------------------------------------------------------------
# Generate a secret: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-here-generate-with-openssl

# -----------------------------------------------------------------------------
# Optional: External API Keys (for plugins)
# -----------------------------------------------------------------------------
# Resend (email plugin): https://resend.com/api-keys
# RESEND_API_KEY=re_xxxxx
